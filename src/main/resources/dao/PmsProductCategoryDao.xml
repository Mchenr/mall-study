<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chenj.mall.dao.PmsProductCategoryDao">

    <update id="updateShowStatus">
        update pms_product_category
        set show_status = #{showStatus, jdbcType = INTEGER}
        where id = #{id}
    </update>

    <update id="updateNavStatus">
        update pms_product_category
        set nav_status = #{navStatus, jdbcType = INTEGER}
        where id = #{id}
    </update>
</mapper>